{
  "private": true,
  "type": "module",
  "scripts": {
   "build": "cd Frontend && npm install && npm run build && cd ../Backend && npm install"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.9.1"
  }
}